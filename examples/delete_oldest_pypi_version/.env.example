# PyPI Credentials
PYPI_USERNAME=your-pypi-username
PYPI_PASSWORD=your-pypi-password-or-api-token
PYPI_PACKAGE_NAME=terminator-py

# Optional: Set to true to run without actually deleting
DRY_RUN=false
